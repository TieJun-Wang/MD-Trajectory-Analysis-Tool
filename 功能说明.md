# MD 轨迹分析工具 —— 功能说明（Web 版 1.0.2）

> 本文档按**界面实际能点到的功能**组织：文件读取 → 体系信息 → 分析对象 → 帧选择 →
> 参数设置 → 计算功能选择 → 运行与实时更新 → 图表 / 数据表 / 统计量 / 科研 QC / 导出。
> 所有参数默认值、取值、输出字段都取自代码（`mdta/pipeline.py` 的 `DEFAULT_PARAMS`、
> 各 `analyze_*` 函数签名），不是凭印象写的。
>
> 架构：Python 分析内核 `mdta/`（6 279 行）+ FastAPI 后端 `webapp/backend/`（1 116 行）
> + React/ECharts 前端 `webapp/frontend/src/`（2 348 行）。分析全部在后端算，前端只画图。

---

## 1. 文件读取

| 功能 | 说明 |
|---|---|
| 拓扑文件 | `.tpr` / `.gro` / `.pdb` / `.psf` / `.prmtop` / `.parm7` / `.xyz` / `.data` |
| 轨迹文件 | `.xtc` / `.trr` / `.dcd` / `.nc` / `.nctraj` / `.xyz` / `.lammpstrj` / `.trj`（可留空＝只读单帧结构） |
| 目录浏览器 | 内建浏览（盘符 → 目录 → 文件），选中拓扑后**自动匹配同名轨迹**（`.xtc/.trr/.dcd/.nc/.trj`） |
| 读取进度 | 阶段式进度条 + 已用时间；**SSE 流式推送**，断流自动退回 400 ms 轮询 |
| 进度阶段 | `打开体系` → `读取拓扑与轨迹` → `解析拓扑与帧数` → `按坐标补全键表` → `补齐对象属性（元素/质量/分子）` → `轨迹已就绪` → `识别组分与主链` → `统计体系信息` → `完成` |
| 键表补全 | `.gro` / 无 `CONECT` 的 `.pdb` / `.xyz` 按共价半径猜键；`.tpr`/`.psf`/`.prmtop` 自带键表则不猜 |
| 对象属性补齐 | 缺失的 `elements` / `masses` / `molnums` 自动补出（分子划分按键连通性），并记录诊断 |
| 偏移缓存重定向 | MDAnalysis 默认会在数据目录旁写 `.<名字>_offsets.*`，只读数据目录下会因 `filelock` **无限重试而死锁**；本工具统一重定向到 `_offsets_cache/`（`MDTA_OFFSETS_INPLACE=1` 可关掉） |

---

## 2. 体系信息

打开后自动切到该页，内容为**结构化表格**（不是定宽文本），共 9 节：

`文件与规模`、`时间轴`、`模拟盒与质量`、`拓扑与属性`、`组分构成`、`终止基团`、
`元素组成`、`各类 residue 数`、`分子/链信息（按组成归类）`。

关键字段：原子数、residue 数、segment 数、帧数、`dt`、首末时刻、总时长、
盒矢量/盒角/盒类型/盒体积、总质量、体系净电荷、键/角/二面角数、是否含电荷/速度、
组分构成与占比、原子名/元素种类数。

**性能说明**：首末时刻走 O(1) 快路径（只定位首末帧），不再为了一份起止时间扫全文件——
46 体系（10 001 帧 / 1.7 GB）实测由 22 s 降到 0.31 s。

---

## 3. 分析对象（主链与组分）

| 控件 | 说明 |
|---|---|
| 主链下拉 | `自动（最大链）` / `组分 <名>` / `<链类型>` / `自定义选择语句`（MDAnalysis 语法，如 `resid 1:200`） |
| 链类型含多条链时 | 给出**两个**独立选项：`全部 N 条链 <标签>（共 X 原子）` 与 `第 1 条（共 N 条，Y 原子）`——前者把所有链合并为一个对象（与选同一组分等价），避免"标着 200 条实际只算 1 条" |
| 组分胶囊 | 参与 RDF / 接触 / 界面 / 密度 / MSD 的组分，点选切换 |
| 分子数显示 | 下拉与胶囊提示里显示每个组分的**分子数**（AdK 实测：protein 1 / water 11 084 / ion 4） |
| 单分子时 | `逐分子统计` 开关**自动置灰**（不给选）：此时整组量就是该分子的量，开关无作用 |
| 界面区组分 | `Analyzer.add_interface_component(A, B, cutoff)` 可把"距 B < cutoff 的 A 原子"定义成新组分，供后续 RDF/取向分析（目前仅在 Python API，未接界面） |

---

## 4. 帧选择（所有分析共用）

| 参数 | 单位 | 留空含义 |
|---|---|---|
| 起始 | ps | 首帧 |
| 结束 | ps | 末帧 |
| 平衡 | ps | 不丢弃平衡段（**建议大于体积/密度平衡时间**） |
| 抽帧 | ps | 每帧都用 |
| 最多帧数 | — | 不限 |

选中后显示 `describe()` 与 `notes`（如"选中 5001 帧，时间范围 0.000 – 100000.000 ps"）。
时间轴本身有**三道质检**：表头 `dt` 为正、实测首末时刻与 `dt×(n−1)` 自洽、
再抽查 5 帧复核；不满足则回退全文件扫描（拼接轨迹/变速输出会被挡下）。

---

## 5. 参数设置（按分析项出现，只显示已勾选项的参数）

> 界面按「分析项 → 参数」分组，三列网格；每个输入框带内联最小宽度，提示文字不会被截断。

### 5.1 回转半径 Rg / 端到端距离 R_ee

| 参数 | 界面名 | 默认 | 取值 / 含义 |
|---|---|---|---|
| `mass_weighted` | 质量加权 Rg | 开 | 开＝质量加权；关＝等权（几何）口径 |
| `per_molecule` | 逐分子统计 | 开 | 开＝逐分子算后再对分子取平均（多分子组分才有"单分子 Rg 平均"等统计量）；关＝只给整组值 |
| `ree_ends` → `ends` | R_ee 链端来源 | `bond_graph` | `bond_graph`（键连图端基 / 蛋白质主链端基）/ `selection`（所选原子组首尾原子，1.0.0 旧口径） |
| `ree_atoms` → `atom_indices` | R_ee 指定原子 | 空 | 如 `0,3034`，逗号或空格分隔；填了就优先用它 |

### 5.2 二面角分析

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `mode` | 二面角模式 | `auto` | `auto`（蛋白质→phi_psi，其它→几何连续原子）/ `phi_psi` / `chain`（键图最长路径）/ `topology`（拓扑定义） |
| `gauche_edge` | trans/gauche 阈值 (°) | 120 | 60–180；**与「结构有序度」共用同一阈值** |

### 5.3 密度分布 / 界面宽度

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `axis` | 密度方向 | 2 (c) | 0=a / 1=b / 2=c |
| `nbins` | 密度 bin 数 | 100（界面 120） | 整数 |
| `mode`（API） | 密度口径 | `mass` | `mass`（g/cm³）/ `number` |

### 5.4 径向分布函数 RDF

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `rmax` | RDF 最大 r (Å) | 12 | 正数（注意 < 半盒长） |
| `nbins`（固定） | bin 数 | 120 | 界面固定发送 120 |
| `rdf_mode` → `mode` | RDF 配对模式 | `inter` | `inter`（分子间，g(r) 标准口径）/ `intra`（同一分子内）/ `total`（含分子内，1.0.0 旧口径） |

三模式**穷尽互斥**（已验证：total 240 = inter 224 + intra 16）；`exclude_bonded` 可剔除 1-2 成键原子对。

### 5.5 接触分析

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `cutoff` | cutoff (Å) | 5.0 | 正数 |
| `contact_mode` → `mode` | 接触配对模式 | `inter` | `inter` / `intra` / `total` |

配对对象自动选：A = 原子数最多的组分，B = **与 A 不同分子**的原子数最多的组分；
若两组落在同一分子（如蛋白与共价糖基被识别成两个组分），自动改用 `total` 口径并写明原因。

### 5.6 链段取向分析

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `orient_mode` → `mode` | 取向链段定义 | `repeat` | `repeat`（化学重复单元，1.0.1 默认）/ `backbone`（几何骨架：键图最长路径，1.0.0 默认）/ `bonds`（所有重原子键） |
| `orient_stride` → `stride` | 几何骨架 stride | 1 | ≥1，仅 `backbone` 模式用 |
| `reference_axis`（API） | 参考轴 | (0,0,1) | 三元组 |

### 5.7 结构有序度分析

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `g_ref` | 局部结构 g_ref | 空 | 留空＝局部结构分量权重强制 0（只由取向+二面角构成） |
| `gauche_edge` | （与二面角共用） | 120 | trans 构象有序度的判据阈值 |
| `weights`（API） | 三分量权重 | (1,1,1) | 按实际参与分量重新归一化 |

### 5.8 键取向序 BOO / 晶体-非晶识别（1.0.2 新增）

| 参数 | 界面名 | 默认 | 取值 / 含义 |
|---|---|---|---|
| `cutoff` | 邻域半径 (Å) | 空（自动） | 留空 = 按「每个原子到最近邻居距离」的中位数 × 1.35 推定，**并跳过 1-2 成键配对**；建议聚合物熔体显式给 ~5 Å |
| `averaged` | 用平均版 q̄_l | 是 | 开 = Lechner–Dellago 平均版（先用邻居的 q_lm 平均再求模，识别固-液更稳）；关 = 单原子 q_l |
| `q6_solid` | 固相判据 q̄6 > | 0.5 | fcc/hcp 约 0.5–0.6，液相约 0.2–0.35（**仅 `crystal` 用**） |
| `min_cluster` | 晶簇最小规模 | 10 | 只有规模 ≥ 此值的空间连通簇才算晶区，滤掉热涨落的零星固相原子（**仅 `crystal` 用**） |

判定思想：`q_lm(i)=⟨Y_lm(r̂_ij)⟩_j`，`q_l=√(4π/(2l+1)·Σ_m|q_lm|²)`；**平均版 q̄_l**
才是识别固-液的稳定判据（液相的单原子 q_l 也有涨落，被邻居平均后会被压低）。
参考相解析值（写进结果说明，便于判断体系离哪个晶型近）：
fcc q4=0.19094/q6=0.57452、hcp 0.09722/0.48476、bcc 0.03637/0.51069、sc 0.76376/0.35355。

`crystal` 流程：q̄6 > `q6_solid` 判为固相原子 → 用邻域连通性做并查集聚类 → 规模 ≥
`min_cluster` 的簇算晶区 → **φ_c(t) = 晶区原子占比**；再对 φ_c(t) 做 **Avrami**
拟合 `ln(−ln(1−X)) = n·ln t + n·ln k`（X 按 (φ−φ0)/(φ1−φ0) 归一化）。
**条件不满足时明确拒绝给 n、k**（体系没结晶、或已结晶完毕时不具备物理意义）。

### 5.9 均方位移 MSD

| 参数 | 界面名 | 默认 | 取值 |
|---|---|---|---|
| `msd_object` → `object` | MSD 追踪对象 | `molecule` | `molecule`（分子质心＝链质心）/ `atom`（原子） |
| `remove_drift`（固定 true） | 去整体漂移 | 是 | — |
| `fit_fraction`（API） | 拟合区间比例 | (0.3, 1.0) | 相对最大 τ |
| `fit_method`（API） | 拟合方法 | `gls` | `gls` / `ols` |
| `n_error_blocks`（API） | 误差分块数 | 5 | ≥3 才不做修正 |

### 5.10 运行区

| 控件 | 默认 | 说明 |
|---|---|---|
| 先预估用时并按短 → 长跑 | 开 | 关掉则按勾选顺序直接跑 |

---

## 6. 计算功能选择（分析功能）

**4 个模块 / 12 个分析项**（与图表导航、数据表、统计量共用同一份分组定义）：

| 模块 | 分析项（内部名 → 标题） |
|---|---|
| **链构象** | `rg` 回转半径 Rg、`ree` 端到端距离 R_ee、`dihedral` 二面角分析 |
| **空间结构** | `density` 密度分布、`rdf` 径向分布函数 RDF、`contact` 接触分析、`interface` 界面宽度分析 |
| **取向与结晶** | `orientation` 链段取向分析、`order` 结构有序度分析、`boo` 键取向序参数 BOO、`crystal` 晶体/非晶区域识别 |
| **动力学与输运** | `msd` 均方位移 MSD |

- 勾选框 + 已选计数 `已选 N / 10`；
- 一排预设按钮：**全选 / 仅构象 / 仅结构 / 仅取向 / 仅输运**，当前勾选正好等于某类时按钮高亮；
- **没有"全不选"按钮**（逐个点掉即可）；
- 勾选后，每项后面会显示 `≈` 预估用时（跑过一次后变为绿色 **实测** 秒数）。

### 运行前先预估用时（1.0.1）

点「开始分析」后先进入 `estimate` 子阶段：每项**各跑几帧实测**，做两点线性拟合分离固定开销与每帧代价，
外推整段用时，再按**短 → 长**排序执行（让结果尽早出现）。帧数 < 40 时自动跳过预估并说明原因。

- 实测（46 体系 10 001 帧）：预估阶段 17 s，占预计总时长 < 0.2%；
- 线性代价项准（RDF 0.89×、接触 1.10×、结构有序度 0.90×）；
- **代价随帧数增长的项会低估**（二面角 49.9×、RDF 2.30×、取向 3.81×），原因见 §12。

---

## 7. 运行与实时更新

| 功能 | 说明 |
|---|---|
| 逐项流式产出 | 后端后台线程逐项算，**每算完一项立即回调**，前端 500 ms 轮询增量接收（只回传尚未收到的结果） |
| 顶栏指示器 | 第一行 `● 实时更新中` + 真实进度条 + `4/10 · 12s`；第二行 `当前分析项：<中文名>（预计 …）· 排队 N 项` |
| 运行面板 | 进度条、后端进度消息、预估用时列表（按短→长，当前项高亮，算完显示实测）、排队列表、取消按钮 |
| 取消 | `POST /run/cancel`：当前这项算完后停下，**已完成的结果全部保留** |
| 运行日志 | 右侧「运行日志」页：逐项耗时、最慢三项、帧选择说明、失败原因 |
| 自动切页 | 第一项算完就切到「图表」并定位到它；点「开始分析」自动收起操作台、展开说明栏 |

---

## 8. 图表展示

- **图表导航**：按模块 → 分析项 → 面板三层；只显示**已有结果**的项（实时运行时一项项长出来）。
- **画法**：ECharts，缩放/平移/hover/图例开关全在前端；曲线类型 `line`（含 `step`）/ `bar`。
- 各分析的面板：

| 分析 | 面板 |
|---|---|
| Rg | Rg–t（含平均线）、Rg 分布、**各分子的平均回转半径**（多分子时） |
| R_ee | R_ee–t、R_ee 分布 |
| 二面角 | 二面角分布 P(φ)、trans/gauche 比例–t、部分二面角–t |
| 密度 | 密度剖面（按组分）、随时间变化 |
| RDF | g(r)（每个组分对一条曲线） |
| 接触 | 接触对数–t、单原子平均接触数–t、配位数分布、接触对数分布、（占据率图） |
| 界面 | 密度剖面与 erf 拟合、宽度–t |
| 取向 | S–t、P2–t、（分布） |
| 结构有序度 | 指数–t、各分量–t |
| BOO | q̄6–t（含 q4/q6/q̄4 各一条）、q̄6 分布、q̄4–q̄6 平面（叠加 fcc/hcp/bcc/sc 参考点） |
| 晶体/非晶识别 | φ_c–t（另叠固相原子占比）、最大晶簇尺寸–t 与晶簇个数–t、Avrami 线性化散点 |
| MSD | MSD–t（双对数可切）、各向异性 MSD、各轴分量 |

- **动态说明栏**：每条说明带**作用域标签**（属于哪张图/哪条曲线）。RDF 跑了 10 个配对、
  图例只勾 1 个时，说明区**只显示那 1 个配对的说明**（按曲线标签前缀自动归属，长标签优先）。

---

## 9. 数据表

按模块 → 分析项 → 面板分组，每个面板一张表（与图表面板一一对应），可整表复制/导出。
列名与曲线一致（如 `距离 r (Å)` / `W-W`）。

---

## 10. 统计量

按同一套模块分组，每个分析项一张两列表（`统计量 / 数值`），**只显示标量指标**（不画图、不导出）。
典型字段：

- Rg：`Rg mean/std/median/min/max`、`Rg sem`、`Rg 块平均标准误`、`分子数`、`统计口径`、
  `单分子 Rg 平均/标准差/最小/最大`；
- R_ee：`R_ee mean/std/sem`、`块平均标准误`、`R_ee 是否可定义`、`链端来源`、`链端原子`、`参与统计的链数`；
- RDF：`第一峰位置/高度 g_max/第一极小位置`、`配位数（第一壳层）`、`第一峰是否显著`、
  `配对模式`、`归一化分母`、`有效原子对数/帧`、`分子内配对占比`；
- 接触：`平均接触对数`、`平均接触数（每个 A 组原子）`、`单原子平均接触数 中位数/最大值`、
  `接触概率是否饱和`、`接触对占据率平均值`、`最持久接触对 #1…#3`、`配对方式/模式`；
- MSD：`D (m²/s)`、`D 标准误`、`D 相对标准误`、`标准误来源`、`残差 AR(1) ρ`、`拟合区间`、
  `R²`、`α (log-log 斜率)`、`拟合可靠性`、`MSD∥/⊥`、`D∥/D⊥`、`最小镜像失效比例`；
- 取向：`S mean/std/sem/min/max`、`P2 mean`、`平均取向矢`、`链段数`、`链段来源`、`参考轴`；
- 结构有序度：`多分量有序度指数 平均/斜率`、三个分量各自的统计量、`权重`、`指数名称`；
- 密度/界面：`体相密度`、`平均密度`、`界面位置`、`界面宽度 10-90`、`erf 拟合 σ/R²`、`界面判据可靠性`；
- BOO：`q4/q6/q̄4/q̄6 平均`、`标准差（原子间）`、`帧平均`、`邻域半径`、`平均邻居数`、`是否用平均版 q̄_l`、`计算后端`；
- 晶体/非晶识别：`结晶分数 平均/首帧/末帧/最大`、`固相原子占比 平均`、`晶簇个数 平均`、`最大晶簇原子数 平均`、`固相判据`、`晶簇最小规模`、`Avrami 是否可拟合` / `Avrami 指数 n` / `Avrami 速率常数 k (1/ns^n)` / `Avrami R²`。

---

## 11. 科研 QC（1.0.1 新增）

把原本散落在各分析模块里的 50 多处判据**统一导出成结构化检查项**，挂在每个结果上：

```python
{"名称": "PBC 追踪", "级别": "bad", "结论": "最小镜像失效比例 24.0%（严重）",
 "依据": "帧间隔内位移超过最小盒高的一半，跨周期追踪不可靠，D 不可引用"}
```

三级语义：`ok` 可引用 / `warn` 数可用但有保留意见 / `bad` **不可引用**。

- 规则位置：`mdta/qc.py::derive_checks`（按 `summary` 标准键名 + `notes` 自动推导，
  **新增分析项只要写标准键名就自动获得 QC 覆盖**）；
- 已覆盖判据：PBC 追踪、扩散标度 α、拟合可靠性、误差棒可信度（分块不足）、样本数与帧数、
  结构峰显著性、接触概率饱和、接触统计有效性（0 接触却近距）、界面判据与 1D 台阶模型适用性、
  量是否有定义（环状 R_ee / 未定义指数）、指数构成与性质（非结晶度）、单粒子各向异性、**邻域定义（BOO：平均邻居数 <4 → 不可引用；<6 → 提示邻域半径偏小）**、
  分析对象与配对口径、质量/键信息缺失；
- **界面**：「科研 QC」标签页。排版同「统计量」——按模块分标签，每个分析项一个可折叠块，
  表格四列**等宽居中**：`QC判断类型` / `QC判断结果` / `QC数据参考` / `解释`；
  行内按严重程度排序（不可引用 → 注意 → 正常），模块内有 `bad` 的分析项排最前；
- **数据字段**：`result.checks`（列表）、`result.checks_digest`（`{ok, warn, bad}` 计数）。

---

## 12. 导出

| 功能 | 说明 |
|---|---|
| 目标目录 | 内建目录选择器；不选则用会话默认输出目录 |
| 导出模块 | 按模块勾选（默认全开） |
| 文件形式 | `CSV 数据表` / `PNG 图` / `Excel 汇总`；`dpi` 可调（默认 200）；`每个面板单独出图` 可选 |
| 逐文件勾选 | 按模块分板块列出**将要产出的每个文件**（表头：选择 / 文件名 / 类型 / 内容），可逐个取消 |
| 文件名规则 | 单面板 `rg.csv` / `rg.png`；多面板 `rg__panel0.csv`；每面板单独出图 `rg__p1.png`；汇总 `summary.xlsx` |
| 打开目录 | 一键在系统文件管理器中打开目标目录 |
| 最近导出 | 显示目录 + 时间 + 文件计数 |

---

## 13. 其他入口

| 入口 | 说明 |
|---|---|
| CLI | `mdta_cli.py`，支持 `--start/--stop/--interval/--max-frames` 等帧选择与分析项选择 |
| 桌面 GUI | `mdta_gui.py`（Tkinter），功能与 Web 版同源 |
| Python API | `Analyzer(topology, trajectory)` → `auto_setup()` → `run(name, params=…)` / `run_all(which, params, order=…)`；`AnalysisResult` 含 `panels/curves/summary/notes/checks` |

---

## 14. 测试与验证

| 套件 | 规模 | 内容 |
|---|---|---|
| `selftest.py` | **54 项** | 数值正确性：Rg 对 MDAnalysis + 解析式三方一致（24.376830 Å）、φ/ψ 对 MDAnalysis 偏差 0.00e+00、RDF 三模式穷尽互斥、4 点水分子内接触恰为 3.000、MSD FFT 对 tidynamics 2.4e-13 / 暴力法 8.2e-14、扩散系数误差棒对真值（OLS 低估 18.2×→修正后 2.04σ）、界面 10–90 = 2.5632σ、QC 规则分级、trans 阈值共享 |
| `webapp/_api_check.py` | 全流程 | TestClient 走完打开→选择→运行→导出→错误处理→关闭；含 PEG 链类型合并回归 |
| `webapp/frontend/_ui_render.mjs` | **306 项** | 服务端渲染断言：布局、参数表宽度、预估列表、QC 四列表、开关置灰… |
| `webapp/_css_check.py` | 构建产物 | 用真实 CSS 数值做静态几何校验（文字是否放得下、按钮是否单行…） |
| `webapp/frontend/_e2e_polymer.mjs` | 29 项 | 前端端到端 |

**文献/基准对照记录**：AdK（Rg 19.65 Å、R_ee 10.198 Å）、PEG9（Rg 对 SANS 基准 ⟨R²⟩₀/M=0.79 Å²·mol/g 差 4.9%；
密度 0.952 vs 实验 1.10 g/cm³ 低 13%；D 2.28e-10 vs PEG400 约 1–3e-11 m²/s）、
md_biopolymer_nowater（癸醇冻结时正确拒绝给 D；ion 最小镜像失效 24% 标为不可引用）。

---

## 14.5 1.0.2 新增：BOO 与晶体/非晶区域识别

- **依赖**：**一个都没加**。`freud`/`pyscal` 等库未安装且本环境无法安装，
  实现按规范写（Steinhardt 1983 的 q_l、Lechner–Dellago 2008 的平均版 q̄_l），
  因此数值可与 freud/pyscal/OVITO 直接对比；`backend="freud"` 仅作**显式**交叉校验通道。
- **验证**：理想晶格复现文献解析值（fcc 0.19094/0.57452、hcp 0.09722/0.48476、
  sc 0.76376/0.35355、bcc 0.03637/0.51069）；理想 fcc 的 φ_c=1.000、随机液体 φ_c=0.0000；
  PEG9 熔体（6000 原子）q̄6=0.168、φ_c=0.000（正确判定不结晶）并拒绝 Avrami。
- **实现踩过的坑（已修，写进代码注释）**：① 自动邻域半径若从"整体尺度"估计会枚举
  近乎全部原子对而卡死；② 不排除 1-2 成键配对时，"最近邻"就是共价键内的原子，
  自动 cutoff 会落到 1.97 Å、平均邻居数只有 2、q6 虚高、把 80% 原子误判成固相
  （现在这种情形会被 QC 判为**不可引用**）；③ hcp 上下三角必须"重叠"而非错开 60°；
  ④ bcc 文献值对应 14 邻居（8 个 ⟨111⟩ + 6 个 ⟨100⟩）。

## 15. 已知限制与待办

| # | 项 | 现状 |
|---|---|---|
| 1 | 界面附近结构 | ⚠️ `add_interface_component` 仅 Python API，未接界面；无按距离分层的 S(z)/g(r\|z) |
| 2 | 块平均标准误覆盖 | ⚠️ 仅 Rg / R_ee / 取向 / 结构有序度有；RDF / 接触 / 密度 / 界面还没有 |
| 3 | 预测耗时对非线性项偏低 | ⚠️ 每帧解包裹代价随帧数增长（n≤21 约 80 ms/帧 → n≥43 约 320 ms/帧），线性外推因此低估（二面角实测 49.9×） |
| 4 | `R_ee` 多分子组分开销 | ⚠️ 上万分子组分逐分子建键图很慢（water 组分分钟级）；正常用法（蛋白/单链）< 1 s |
| 5 | 结果溯源 | ⚠️ 导出目录未内嵌 `params/frames/版本` 的 metadata（`export.py` 不写） |
| 6 | 打包 | ❌ 无 `pyproject.toml` / `requirements.txt` |
| 7 | 其他分析空缺 | 集体输运（NE/EH/Onsager/Haven）、氢键寿命/停留时间、笼效应 C(t)、PCA、`.edr` 读取、Yeh-Hummer 有限尺寸扩散修正 |

---

## 附：改前端文字后为什么"没效果"

```
webapp/frontend/src/**      ← 改这里（QcPanel.jsx / styles.css …）
        │  cd webapp\frontend && node _build.mjs
        ▼
webapp/frontend/dist/**     ← 后端真正托管的就是这里（FastAPI StaticFiles）
        │  刷新浏览器（bundle 哈希命名，刷新即取新文件）
        ▼
      页面
```

- 改**前端文字/样式** → 必须 `node _build.mjs` 重新构建 + 刷新页面（**后端不用重启**）；
- 改**后端数据/字段**（如 `mdta/qc.py` 的 `结论`/`依据`）→ 必须**重启后端**；
- 想改完即时生效：在 `webapp\frontend` 下 `npm run dev` 起 Vite 开发服务器（带 HMR，`/api` 自动代理到后端）。
